namespace Minecraft1_8_9Port.net.minecraft.client.resources.data;

public class IMetadataSerializer
{
    //TODO: finish: Registration implement: metadataSectionSerializerRegistry, jsonBuilder, registerMetadataSectionType, parseMetadataSection, getJson

    private class Registration<T> : IMetadataSection
    {
        //public IMetadataSectionSerializer<T> section;
        //public Type<T> clazz;

        /*
        public Registration(IMetadataSectionSerializer<T> metadataSectionSerializer, Type<T> clazzToRegister)
        {
            this.section = metadataSectionSerializer;
            this.section = clazzToRegister;
        }
        */
    }
}